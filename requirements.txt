# SearXNG workspace — pinned Python dependencies
# Generated from: pip freeze (verified working environment)
# Regenerate after any dependency change and test thoroughly before deploying
#
# Python version requirement: >= 3.12, tested on 3.14
# Virtual environment recommended: python -m venv .venv && source .venv/bin/activate

# ── Core RAG ────────────────────────────────────────────────────────────────────
qdrant-client==1.18.0
fastembed==0.8.0

# ── MCP servers ─────────────────────────────────────────────────────────────────
fastmcp==3.4.3
pydantic==2.13.4

# ── Sync daemon & API clients ──────────────────────────────────────────────────
requests==2.34.2
docker==7.1.0
